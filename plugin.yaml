name: browser_session_sync
version: 1.4.0
title: Browser Session Sync
description: Persists native browser tabs, cookies, and localStorage across container restarts with one-shot startup restore and debounced live-state tracking.
always_enabled: false
per_project_config: false
per_agent_config: true
settings_sections:
  - plugins
